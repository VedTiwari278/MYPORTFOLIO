<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Education</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gray-900 text-white font-[Inter]">
    <div class="container mx-auto px-4 py-16">
      <h2
        class="text-4xl font-bold text-center mb-12 bg-gradient-to-r from-pink-500 to-yellow-400 bg-clip-text text-transparent"
      >
        Education
      </h2>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-10">
        <% data.forEach(edu => { %>
        <div
          class="bg-gray-800/50 p-6 rounded-xl backdrop-blur-sm border border-gray-700 hover:scale-[1.02] hover:shadow-2xl transition-all duration-300 ease-in-out hover:border-pink-500"
        >
          <h3 class="text-xl font-semibold text-pink-400 mb-1">
            <%= edu.degree %>
          </h3>
          <p class="text-sm text-gray-400 mb-2"><%= edu.institute %></p>
          <p class="text-gray-300"><%= edu.session %></p>
        </div>
        <% }); %>
      </div>
    </div>
  </body>
</html>
