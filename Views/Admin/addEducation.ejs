<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add Education</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body
    class="bg-gray-900 text-white font-[Inter] min-h-screen flex items-center justify-center px-4"
  >
    <div
      class="bg-gray-800/60 p-8 rounded-xl w-full max-w-xl shadow-lg border border-gray-700"
    >
      <h2
        class="text-3xl font-bold text-center mb-6 bg-gradient-to-r from-blue-400 to-green-400 bg-clip-text text-transparent"
      >
        Add Education
      </h2>

      <form action="/admin/add-education" method="POST" class="space-y-5">
        <div>
          <label class="block mb-1 font-medium text-sm">Degree</label>
          <input
            type="text"
            name="degree"
            required
            class="w-full px-4 py-2 rounded-lg bg-gray-700 text-white border border-gray-600 focus:outline-none focus:ring-2 focus:ring-blue-400"
            placeholder="e.g. B.Tech in Computer Science"
          />
        </div>

        <div>
          <label class="block mb-1 font-medium text-sm">Institute</label>
          <input
            type="text"
            name="institute"
            required
            class="w-full px-4 py-2 rounded-lg bg-gray-700 text-white border border-gray-600 focus:outline-none focus:ring-2 focus:ring-blue-400"
            placeholder="e.g. IIT Bombay"
          />
        </div>

        <div>
          <label class="block mb-1 font-medium text-sm">Session</label>
          <input
            type="text"
            name="session"
            required
            class="w-full px-4 py-2 rounded-lg bg-gray-700 text-white border border-gray-600 focus:outline-none focus:ring-2 focus:ring-blue-400"
            placeholder="e.g. 2020 - 2024"
          />
        </div>

        <button
          type="submit"
          class="w-full bg-blue-500 hover:bg-blue-600 transition text-white py-2 rounded-lg font-semibold"
        >
          Add Education
        </button>
      </form>
    </div>
  </body>
</html>
